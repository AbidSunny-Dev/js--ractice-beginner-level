const person={

    name:'Abid Ahmed Sunny',
    age:25,
    gender:'male',
    gpa:'4.00',




}

const keys=Object.keys(person);
console.log(keys);

const values = Object.values(person);
console.log(values);